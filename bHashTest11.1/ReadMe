11.1版本是11.0的比较版本，对11.0版本作了工程上的改动。

改动：将small group中的kv-pair单独放在一个文件spill中，而不是像之前一样附加在结果文件的尾端。

如果available memory内存很小时， 根据情况输出索引不能太大，此时计算得到的big group只有10%（此时spill文件占整个文件的90%），大部分是在通过分组hash实现的，
比较符合预期效果。

