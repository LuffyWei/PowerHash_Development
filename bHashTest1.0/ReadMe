原始版本的bHash,因此命名为1.0版本。
bHash算法分两步: 1.遍历原始数据集，为每个group建立输出索引，索引的建立是整个算法的核心。我们首先计算每个group的精确大小，然后通过累加的形式获取每个group的索引
               2.通过索引，将各个组的kv-pair输出到结果文件。
bHash算法设计时，就将原始文件通过hash分成几个小文件，这是为了在第二步输出kv-pairs时缩小seek的范围。

算法存在的问题：随着数据集的增大，数据集中group数目增多，索引就会变得特别大，容易引起out-of-memory问题